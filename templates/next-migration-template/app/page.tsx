"use client";

export default function Home() {
  return (
    <main className="page-shell">
      <section className="hero-card">
        <p className="eyebrow">Next Migration Template</p>
        <h1>把静态页面迁移到这里</h1>
        <p>
          将原 HTML 拆成 JSX，把原 CSS 放入 <code>app/globals.css</code>，把原 JS 交互改成 React state。
        </p>
      </section>
    </main>
  );
}
